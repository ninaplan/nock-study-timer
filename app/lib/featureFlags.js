/**
 * When `true`, free accounts see locked Pro UI (stats/chart limits).
 * When `false`, the app behaves as if everyone has Pro — turn on again to lock in one place.
 */
export const PREMIUM_GATES_ENABLED = false;
